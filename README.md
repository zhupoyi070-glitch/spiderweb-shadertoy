# SpiderWeb — Shadertoy 4fc3zf 本地复现

原作者:**middle**
原作地址:https://www.shadertoy.com/view/4fc3zf
授权:Shadertoy 默认 **CC BY-NC-SA 3.0**(以原作者页面标注为准),本目录仅用于本地学习/研究。

一只白蜘蛛挂在物理模拟的蛛网上。蛛网是真正的**布料物理**:10×7 节点网格做
Verlet 积分,胡克定律弹簧约束,带重力、摩擦、回弹;蜘蛛的 8 只脚被弹簧拉向身体质心。
**按住鼠标拖动可以拉扯蛛网和蜘蛛**。全部由 GPU 着色器实时计算,无外部资源。

## 在线游玩

**https://zhupoyi070-glitch.github.io/spiderweb-shadertoy/**(GitHub Pages,打开即玩)

## 怎么运行(本地)

**方式一(最简单):双击 `index.html`** —— 着色器已内嵌,单文件运行,无需服务器。

**方式二:** 双击 `start.bat`(或 `python -m http.server 8767` 后打开 http://localhost:8767/)。

## 操作

- **按住鼠标拖动:拉扯蛛网**(原作交互,基于 Shadertoy 的 iMouse 点击语义)
- 空格:暂停 / 继续
- R 或「重置」:重置物理模拟
- P:参数面板开关

## 参数面板

- **模拟步速(步/帧)**:物理子步数,调大可让物理更细腻/时间更快。
- **渲染精度**:降低可提速(会重置模拟)。
- **浮点缓冲**:Verlet 积分需要精确的"上一帧位置"来算速度,8 位缓冲会量化毁掉物理,
  因此默认 RGBA16F(需 `EXT_color_buffer_float`,自动请求)。勾掉可观察退化。
- **弹簧刚度 K**(0.005~0.2):蛛网的硬度。调大网更紧绷,调小网松软下垂。
- **摩擦阻尼**(0~0.3):摆动衰减速度。0 = 永不停止的荡秋千。
- **重力强度**(0~10):0 = 蜘蛛失漂太空;10 = 网被压得大幅下垂。
- **蛛网间距 R1**(0.01~0.06):网眼大小,拖动时整张网实时缩放。
- **拉网力度**(0~1.5):点击/拖动鼠标后拉扯蛛网的力度。

前五项物理参数对应 `common.frag`/`bufferA.frag` 里的 uniform(`K/Friction/Gravity/R1/uMouseK`),
默认值 = 原作常量,实时生效无需重置。
每个参数旁有 **「?」提示按钮**,悬停或点击会弹出该参数的效果说明。

## 🎮 关卡模式(6 关全部实装)

点击左下角「🕷️ 关卡模式」进入游戏。顶部任务条:可见行只有**悬念式提示**(不泄露做法),
下方两枚提示按钮:「💡 提示1」给方向性引导(部分关卡的引导数值会实时跳动),
看过提示1后解锁「🔒 提示2」——**具体通关标准 + 实时进度**(如"已点击 3/15 次"
"失重保持 2.1/5.0 秒""往复 4/6")。通关即播放**两波马赛克像素烟花**,并弹出
「🎓 教学时间」对话框讲解本关物理原理,点击「完成」重置物理与参数后进入下一关
(严格按顺序,不可跳关)。

| 关 | 名称 | 玩法与通关标准 | 物理原理 |
|---|------|--------------|---------|
| 1 | 第一次触碰 | 点击并拖动鼠标;最大节点位移 > 0.050 即通关 | 拖动 = 胡克定律弹力,位移沿弹簧链传播 |
| 2 | 不安的蜘蛛 | 连续点击蜘蛛 15 次(点中 90px 内),蜘蛛焦虑抖动加剧(幅度/频率随焦虑同步放大);满 15 次后蜘蛛**脱离蛛网、沿一条蛛丝直线爬向屏幕外**(腿部中幅摆动、频率随焦虑升高),**蛛网保持静止**,蜘蛛完全越出屏幕边缘即通关 | 焦虑值调制正弦扰动的振幅与频率;爬行 = 质心沿固定方向的匀速运动 + 腿部相位差摆动,弹簧链的记忆留在原地 |
| 3 | 太空失重 | 重力强度 ≤ 0.5 并保持 5 秒 | 重力是每步加进速度的恒定加速度 G·dt²/2,归零后只剩弹簧回弹力,蛛网回到无重力形状 |
| 4 | 软网实验室 | K ≤ 0.015 且摩擦 ≤ 0.01 保持 1 秒,再拖动蛛网(最大位移 > 0.050) | k 决定回复力强弱:k 小弹簧「懒」;摩擦小 = 能量几乎不耗散,网以固有频率长时间振荡 |
| 5 | 呼吸的网 | 10 秒内蛛网间距 R1 往复 ≥ 6 次(幅度 ≥ 0.02) | R1 是弹簧自然长度,胡克力正比于(实际长度-自然长度):改 R1 = 瞬间改变平衡态,网收缩/扩张去追赶 |
| 6 | 毕业:失重蹦迪 | 重力 ≤ 0.5 状态下,10 秒内拉网力度往复 ≥ 6 次(幅度 ≥ 0.25) | 失重消掉恒定下拉力后,拉网力度成为周期性外力 = 对弹簧系统做共振驱动,振幅越荡越大 |

每次通关自动:重置物理 + 参数恢复本关预设。

## 技术说明

- 三个 pass:**Buffer A**(Verlet 物理,自反馈 ping-pong)+ **Image**(画蛛网线段与蜘蛛)+
  **Common**(共享宏:网格尺寸、弹簧系数 K、摩擦、重力、蛛脚位置表)。
  本运行页已支持 Common 注入(拼接到每个 pass 前面)。
- 节点状态存于 Buffer A 纹理:xy = 当前位置,zw = 上一帧位置(Verlet 求速度用),
  因此**必须浮点缓冲**。
- 稳健性修复(不影响视觉):
  - `HukeLaw` 中 `length(AB)` 加下限,防止两节点重合时 0/0=NaN 污染整个状态纹理;
  - 位置 `clamp(±5)`,防止激振后发散;
  - 运行页对 `iTimeDelta` 钳制 50ms 上限,防止标签页节流后首帧大 dt 引爆重力项。
- 物理量级:节点间距 `R1=0.02`,弹簧 `K=0.05`,摩擦 0.03,重力 `(0,-3)`。

## 文件清单

- `index.html` —— 单文件运行版(双击即玩,或访问上方在线链接)
- `index.template.html` —— 运行时模板(不含着色器源码)
- `shaders/image.frag` / `shaders/bufferA.frag` / `shaders/common.frag` —— 原作源码
- `_shader.json` —— Shadertoy 官方接口原始数据留档
- `build.py` —— 重建脚本:`python build.py`
- `start.bat` —— 可选的本地服务启动脚本

## 修改记录

- **v9 界面风格统一**(2026-09-18):参数面板缩小并改为纯黑白灰配色;滑杆拖钮换成
  小蜘蛛造型(SVG);关卡开关移至左下角并放大;移除左下角蛛网贴图与右上角绿色 HUD;
  通关烟花改为在前排播放(教学对话框上方也能看到)。

- **v8 两段式提示**(2026-09-18):提示拆成两档——提示1(方向引导,部分带实时数值,
  如 L2 的"不安 x%")与提示2(具体通关标准+实时进度,看过提示1才解锁);
  任务栏改为点击穿透,不再挡住蜘蛛的点击。

- **v7 关卡模式 6 关全部实装**(2026-09-18):提示栏重构(可见行=悬念形容,提示按钮=具体
  标准+实时数值);L3 太空失重 / L4 软网实验室 / L5 呼吸的网 / L6 毕业失重蹦迪判定实装;
  L1/L4 判定改用最大节点位移;L2 完全离场判定(质心越界 150px);新增 __gtick 同步测试钩子。

- **v6 交互修复与教学对话框**(2026-09-18):修复通关提示按钮失效(tbShow 每帧重置
  导致展开即被压回);通关后在烟花中弹出「🎓 教学时间」对话框,从物理角度讲解本关
  原理(质点-弹簧/胡克定律/共振等),点击「完成」才进入下一关;L2 爬行离场时停止
  焦虑抖动,腿部改为一前一后交替摆动(步行姿态);关卡开关按钮加大并移向屏幕中下方。
- **v5 UI 与爬行重做**(2026-09-18):关卡开关移至左下角(黑白主题+角落蛛网装饰);
  任务条放大并加蛛网外框角纹;通关提示改为「💡 查看通关提示」按钮按需显示;
  L2 逃离改为蜘蛛沿一条蛛丝直线爬出画面(蛛网静止,腿部中幅摆动,越出边缘即通关);
  烟花加至两波 9 簇。
- **v4 关卡模式第 1~2 关**(2026-09-18):任务条 + 马赛克烟花 + 严格顺序关卡框架;
  L1 触碰(节点位移判定);L2 不安的蜘蛛(点中 90px 内计数、焦虑驱动抖动幅度/频率、
  15 次后自动拉满力度沿蛛网爬出画面)。修改前代码存于 `pre-game` 分支。
- **v2 物理参数面板**(2026-09-18):把 `common.frag` 的 K/Friction/Gravity 和
  `bufferA.frag` 的 R1 由编译期常量改为 uniform,新增 5 个滑杆
  (弹簧刚度/摩擦阻尼/重力强度/蛛网间距/拉网力度)实时调节,默认值 = 原作常量。
- **v1**(2026-09-17):数据提取 + 支持 Common 注入的 ping-pong 运行页;
  浮点缓冲与三项稳健性修复(NaN 防护、位置钳制、dt 钳制)。
